{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "TypeScript does not follow typical npm semver and introduces breaking changes on minor releases, instead of strictly major version updates. This preset will workaround this by setting renovate to pin TypeScript versions in devDependency",
  "packageRules": [
    {
      "depTypeList": ["devDependencies"],
      "updateTypes": ["patch", "minor"],
      "matchPackageNames": ["typescript"],
      "rangeStrategy": "pin"
    }
  ]
}
