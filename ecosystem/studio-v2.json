{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Stay up to date to Sanity Studio v2, and update plugins in a single PR",
  "packageRules": [
    {
      "description": "Sanity studio isn't ready for React v18 yet",
      "matchPackagePrefixes": ["react", "@types/react"],
      "matchDepTypes": ["dependencies", "devDependencies", "resolutions"],
      "allowedVersions": "<=17",
      "enabled": true
    },
    {
      "description": "@sanity/ui@0.38 and newer requires a higher react version than 16, making it a breaking change",
      "packageNames": ["@sanity/ui"],
      "matchDepTypes": ["dependencies", "devDependencies", "resolutions"],
      "allowedVersions": "<=0.37",
      "rangeStrategy": "pin",
      "enabled": true
    },
    {
      "description": "@sanity/icons@1.3.6 and newer requires a higher react version than 16, making it a breaking change",
      "packageNames": ["@sanity/icons"],
      "matchDepTypes": ["dependencies", "devDependencies", "resolutions"],
      "allowedVersions": "<=1.3.5",
      "rangeStrategy": "pin",
      "enabled": true
    }
  ]
}
